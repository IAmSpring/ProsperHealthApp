{
  "name": "@prosper/db",
  "version": "0.1.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "generate": "prisma generate",
    "push": "prisma db push",
    "seed": "ts-node --transpile-only seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.9.0",
    "prisma": "^6.6.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
